# Archivos de desarrollo — no se publican en la web
_fix.py
_standby/

# Backups locales (copias .bak antes de editar) — no publicar
*.bak
*.bak-*
*.bak_*

# Nota interna de operacion (Claude) - NO publicar en la web
CLAUDE.md
